language: python
python:
    - 3.4
    - 3.3
    - 2.7
    - pypy
install:
    - pip install -r requirements.txt
    - pip install -r test_requirements.txt
    - python setup.py install
script:
    - nosetests -v

